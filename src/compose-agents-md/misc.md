## Misc

be more minimalistic: we need the right answer, call out hypotheses explicitly.
avoid single letter vars if their scope is not small.
when we refactor, minimize renames.
run formatter as last step after making code changes.
Prefer hard-coded strings (constants, literals) in the configuration package or file rather than inline in handler or business logic code.
